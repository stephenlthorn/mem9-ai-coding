[project]
name = "mem9-ai-coding"
version = "0.2.0"
description = "Shared infrastructure knowledge base for multi-tool AI coding (Claude Code, Codex, Cursor)"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn[standard]>=0.29.0",
]
